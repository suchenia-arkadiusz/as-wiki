name: Test Action
run-name: ${{ gitea.actor }} is testing out Gitea Actions
on: [push]

jobs:
    Check-NVM:
        runs-on: local-runner
        steps:
            - run: >
                  . $NVM_DIR/nvm.sh
                  nvm.sh --version
                  docker --version
export type TSignInFormValidated = {
  username: boolean;
  password: boolean;
};

export type TSignUpFormValidated = {
  username: boolean;
  password: boolean;
  confirmPassword: boolean;
  email: boolean;
  firstName: boolean;
  lastName: boolean;
};
